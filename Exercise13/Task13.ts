// TASK #13 (Your Own Array:)
let FavTransportation:string[] = ["G wagon", "Range Rover", "Honda city"];
let statement:string = "I would like to own a"
FavTransportation.map((item)=> console.log(`${statement} ${item}`));